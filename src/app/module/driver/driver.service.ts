import { prisma } from "../../lib/prisma";
import { AppError } from "../../utils/AppError";
import httpStatus from "http-status";

const applyAsDoctor = async (payload: any) => {
 
};

export const DriverService = {
  applyAsDoctor,
};
